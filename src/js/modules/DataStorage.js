export default class DataStorage {
    constructor(options) {
        this.options = options;
        // this.saveStorage()
    }


    saveStorage = (event) => {
        // console.log(this.newsApi.getCardsNews().then(res => { console.log(res.artecles) }))
        // console.log(this.options.searchInput.searchNewsSubmit(event))

        // this.options.newsApi.getCardsNews().then(res => console.log(res))
    }
}